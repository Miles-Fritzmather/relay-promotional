# Copy to .env.local and fill in from https://dashboard.clerk.com
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Clerk Dashboard → User & Authentication → Waitlist: enable "Waitlist" so <Waitlist /> works.
# waitlistUrl is set to "/" in app/layout.tsx (form lives in the modal on the home page).
